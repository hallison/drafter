author = "Hallison Batista"
version = "0.1.0"
files = {
  "bin/{app.name} = lua/script",
  "lib/{app.name}.lua = lua/module.lua",
  "rockspecs/{app.name}-{app.version}.rockspec = lua/rockspec",
  "README.md = lua/README.md",
}
